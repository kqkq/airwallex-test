{
  "name": "aw",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node app.js",
    "test": "nyc --reporter=html --reporter=text --report-dir=./reports/coverage mocha tests/*.js tests/**/*.js --exit --timeout 30000 --reporter mochawesome --reporter-options reportDir=./reports/test,reportFilename=index"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.3.4",
    "mocha": "^8.3.2",
    "mochawesome": "^6.2.2",
    "nyc": "^15.1.0"
  }
}
